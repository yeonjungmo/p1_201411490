import math
def isinCircle(curpos,radius,pos):
     t1.circle(radius)
     pa=math.sqrt(math.pow(curpos[0]-pos[0],2)+math.pow(curpos[1]-pos[1],2))
     if(pa>radius):
         print true
     else(pa<=radius):
         print fales

	